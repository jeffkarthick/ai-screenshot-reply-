import "./globals.css";

export const metadata = {
  title: "ReplyAI — What Should I Reply?",
  description: "Upload a screenshot and get AI-powered reply suggestions."
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}